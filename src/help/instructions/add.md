**`add` - Add registers or immediate values to a register**

The `add` instruction adds either an immediate value or another register's contents to a register. Over- and underflows will not set any flags.